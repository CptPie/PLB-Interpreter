# PLB-Interpreter
This will (hopefully) end up as a working interpreter for the ANSI spec of the PL/B programming language.

## Why?
Honestly idk, i figured i should get some use out of the "Writing a {compiler|interpreter} in Go" Books and why not try it on the worst language imaginable. 

